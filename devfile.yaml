schemaVersion: 2.2.0
metadata:
  name: ansible-automation-space

components:
  - name: ansible-runtime
    container:
      image: quay.io/vifernan/dev-ocp-ansible:latest
      memoryLimit: 3G
      cpuLimit: '1'
      mountSources: true
      attributes:
        editor:
          type: vscode
